# FRONTEND ENVIRONMENT CONFIGURATION
# This file is used by the frontend to locate the backend API.
# Note: For static HTML deployment, these values are also mirrored in window.V2I_CONFIG in index.html.

V2I_BACKEND_API_URL=http://localhost:8081
V2I_BACKEND_API_KEY=v2i_client_auth_secret_demo
